// ── Shared season configuration ──────────────────────────────────
// Both index.md and season.md import `OVERRIDE_YEAR` from here
//
// By default (OVERRIDE_YEAR = null) each page auto-detects the
// current season as the newest year present in its own water-
// temperature data. 
//
// Set OVERRIDE_YEAR to a specific year (e.g. 2027) only if you need
// to pin the site to a particular season regardless of what's in
// the data 

export const OVERRIDE_YEAR = null;